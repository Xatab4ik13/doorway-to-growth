## План правок (без хаоса, по этапам)

Разделил всё на **6 этапов**. После каждого — пауза, вы говорите «дальше».

---

### Этап 1 — Условия (срок, доставка, гарантия, замер, рассрочка)

Файлы: `src/pages/StorefrontProduct.tsx`, `src/pages/StorefrontCart.tsx`, возможно `StorefrontBrand.tsx`, футер.

- Срок изготовления: **25 рабочих дней** (заменить везде, где упоминается срок).
- Доставка по Москве: **3500 ₽** (фикс. сумма, убрать «от 1500 ₽»).
- **Убрать упоминание рассрочки** полностью.
- Гарантия: **1 год** (заменить блок «10 лет / 5 лет / 2 года»).
- Замер: **платный** (убрать «бесплатно», в т.ч. в `StorefrontProduct.tsx:1082` и в брендовой странице).

### Этап 2 — Размеры коробов в спецификациях

База, миграция данных (UPDATE через insert-tool):

- ESTETICA: указать короб **36 × 85** (сейчас поле пустое в `specifications.frame`).
- PRIME: указать короб **36 × 75**.
- Отобразить значение в карточке товара в блоке «Характеристики» (если ещё не выводится).

> Уточнение: запись «36/85» и «36/75» — это толщина/ширина коробочного бруса, верно? Если да — добавлю в `specifications.frame` как строку, и выведу в UI.

### Этап 3 — Добавить недостающие модели PRIME

Сейчас в БД есть: 01, 02, 05, 06, 07, 08, 09, 11, 12, 13, 20, 21, 22, 23, 24, 28.
Вы просите добавить: **40, 48, 14, 12, 26, 29, 31, 35, 27, 32**.
12 — уже есть в БД. Остальные (40, 48, 14, 26, 29, 31, 35, 27, 32) — создам как новые товары PRIME, скопирую структуру `specifications` с существующей PRIME-модели (цвета, размеры, остекление), фото возьмём с **primedoor.ru** на следующем этапе.

> Уточнение: «PRIME 40» — это та же коллекция PRIME, или это отдельная серия (например, «PRIME 40 мм»)? Подтвердите — добавлю как PRIME.

### Этап 4 — Цвета PRIME

Добавить в `specifications.colors` для всех моделей PRIME:
- Орех натуральный
- Орех Pacific
- Орех Boho

Свотчи цвета (превью-картинки) — возьму с primedoor.ru, загружу в Storage, привяжу через `product_images.variant_key`.

### Этап 5 — Входные двери: фильтры + панели + картинки

5.1. **Фильтры каталога для входных дверей** (`StorefrontCatalog.tsx`):
- Убрать секцию «Погонаж» и «Фурнитура» из фильтров, когда `lockedParent = «Входные двери»`.
- Убрать «Систему открывания» из фильтров для входных.
- Эти же секции (Погонаж / Фурнитура / Открывание) **полностью убрать с карточек входных дверей** в `StorefrontProduct.tsx` (показывать только для межкомнатных).

5.2. **Выбор панели на входных** (`StorefrontProduct.tsx`):
- Добавить новый блок конфигуратора «Панель» — список панелей с превью, при выборе меняется `variant_key` и подгружается соответствующее фото.
- Данные панелей — в `specifications.panels` (массив `{id, name, image_variant_key}`).

> Уточнение: панели — это **внутренняя отделка** входной двери? Сколько вариантов панелей в среднем, и они одинаковые для всех 10 моделей входных, или у каждой свой набор?

5.3. **Обрезанные картинки входных дверей**:
- Сейчас в карточке `object-contain` + `max-h-66vh` — но фото с CRM, видимо, неправильного формата. Проверю реальные URL фото, у обрезанных — заменю на полноразмерные с primedoor.ru через Storage.

### Этап 6 — Контент с primedoor.ru

Скрипт-парсер (Firecrawl + downloader) для:
- Карточек PRIME 14, 26, 27, 29, 31, 32, 35, 40, 48 — фото моделей.
- Свотчи цветов «Орех натуральный / Pacific / Boho».
- Фото панелей для входных дверей (если на primedoor.ru есть).
- Полноразмерные фото входных дверей (BR 01/02, TERMO BR 101–105).

Все картинки → WebP 800px → Supabase Storage `product-images` → `product_images` с привязкой `variant_key`.

---

**Порядок:** 1 → 2 → 5.1 (быстрые правки кода) → 3 → 4 → 5.2 → 6 → 5.3 (всё, что требует фото).

Ответьте на 3 уточнения выше (короб = брус? PRIME 40 = коллекция PRIME? панели входных) — и начну с Этапа 1.